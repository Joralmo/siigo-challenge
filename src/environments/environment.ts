// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  siigoApi: 'https://api.siigo.com/v1',
  siigoAuthApi: 'https://services.siigo.com/alliances/api/siigoapi-users/v1',
  crossOriginApi: 'https://cors-anywhere-jp.herokuapp.com',
  enableCross: true,
  defaultLanguage: 'es',
  username: 'pilotoapi@empresa.com',
  accessKey: 'YjE0YWUyYjMtNGJlNS00YTdhLTgzZDMtYjFhMzM3MjkyOTBhOm5hYSV5VVk2c1Y=',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
